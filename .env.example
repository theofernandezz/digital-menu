NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Real admin credentials — used by the integration and e2e test suites
# (application/**/*.integration.test.ts, e2e/*.spec.ts) and by CI, which
# reads these as GitHub Actions secrets.
ADMIN_EMAIL=
ADMIN_PASSWORD=
